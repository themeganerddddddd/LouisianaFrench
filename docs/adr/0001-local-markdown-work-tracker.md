# Use committed local Markdown for work tracking

Engineering specs and tickets are committed under `.scratch/` rather than stored in GitHub Issues. This keeps the complete dependency graph and agent-ready context versioned beside the code while GitHub pull requests remain the review and merge surface.
